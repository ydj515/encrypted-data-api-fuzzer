spring:
  application:
    name: report-server
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 48080

report:
  data-dir: ${REPORT_DATA_DIR:${user.dir}/data/runs}
