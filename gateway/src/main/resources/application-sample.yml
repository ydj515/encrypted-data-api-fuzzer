spring:
  application:
    name: gateway

server:
  port: 28080

gateway:
  apis:
    - org: exampleOrg
      service: ExampleService
      api: search
      method: POST
      host: https://x.x.x.x
      externalPath: /api/~~~
      key: secret-key
      apiKey: change-me
      insCode: insCode
